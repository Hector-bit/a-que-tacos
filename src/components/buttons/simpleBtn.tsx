'use client'

interface SimpleBtnProps {

}


// might not need this but button css hover was being funky and not loaded on some renders, then it just started working 
// but I might need this again if I need more button interactivity
const SimpleBtn = ({}:SimpleBtnProps) => {
  return (
    <button>

    </button>
  )
}

export default SimpleBtn;